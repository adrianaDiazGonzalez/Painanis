import { Component } from "@angular/core";

@Component({
  selector: "main",
  template: `
  <ScrollView>
   <StackLayout>
    <ActionBar title="Agregar" class="action-bar"></ActionBar>
    
     </StackLayout>
  </ScrollView>
  `
})

export class Agregar {
  
    
}
